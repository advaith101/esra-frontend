<template>
    <div v-if="$store.getters.isLoading" class="loader-overlay">
        <div class="loader"></div>
        <span class="text" v-html="text"></span>
    </div>
</template>

<script>
    export default {
        name: "Loader",
        props: {
            isVisible: {type: Boolean, required: true},
            text: {type: String, required: false, default: "Loading...", value:"Loading..."},
        },
}
</script>

<style lang="scss" scoped>
    .loader-overlay {
        position: fixed;
        width: 100%;
        height: 100%;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-color: rgba(0, 0, 0, 0.7);
        z-index: 999;
        cursor: pointer;
        span.text {
            display: inline-block;
            position: absolute;
            top: 50%;
            left: 50% !important;            
            transform: translate(-50%,-50%);
            color: #4CB3D2;
            font-size: 16px;
        }
        .loader {
            animation: loader-animate 1.5s linear infinite;
            clip: rect(0, 80px, 80px, 40px);
            height: 80px;
            width: 80px;
            position: relative;
            left: calc(50% - 40px);
            top: calc(50% - 50px);
            &:after {
                animation: loader-animate-after 1.5s ease-in-out infinite;
                clip: rect(0, 80px, 80px, 40px);
                content: '';
                border-radius: 50%;
                height: 80px;
                width: 80px;
                position: absolute;
            }
        }
        @keyframes loader-animate {
            0% {
                transform: rotate(-80deg)
            }
            100% {
                transform: rotate(-80deg)
            }
        }
        @keyframes loader-animate-after {
            0% {
                box-shadow: inset #FFB84E 0 0 0 5px;
                transform: rotate(-140deg);
            }
            50% {
                box-shadow: inset #FFB84E 0 0 0 5px;                
            }
            100% {
                box-shadow: inset #FFB84E 0 0 0 5px;
                transform: rotate(140deg);
            }
        }
    }
</style>